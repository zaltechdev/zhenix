# AI boundary

The Vercel AI SDK belongs here behind a server-only provider abstraction. Validate model output and tool arguments with Zod before any future orchestration work.

No model call or agent loop is implemented in the foundation.
